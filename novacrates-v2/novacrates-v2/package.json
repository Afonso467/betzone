{
  "name": "novacrates",
  "version": "1.0.0",
  "description": "NovaCrates Gaming Platform — monorepo root",
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\" --names \"API,WEB\" --prefix-colors \"yellow,cyan\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend",
    "migrate": "npm run migrate --prefix backend",
    "seed": "npm run seed --prefix backend",
    "setup": "npm run install:all && npm run migrate && npm run seed",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "gh-pages": "^6.3.0"
  }
}
